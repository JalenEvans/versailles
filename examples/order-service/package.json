{
	"name": "order-service-example",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"typecheck": "tsc --noEmit",
		"test": "vitest run",
		"verify": "tsc --noEmit && vitest run"
	},
	"devDependencies": {
		"fast-check": "^4.9.0"
	}
}
